var React = require("react");
document.addEventListener(
    "DOMContentLoaded",
    function(event) {
        React.render(
            <h1>hello</h1>,
            document.getElementById('content')
        );
    }
)

